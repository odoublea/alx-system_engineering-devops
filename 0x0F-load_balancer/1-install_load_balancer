#!/usr/bin/env bash
# Install and configure HAproxy on load-balancer

sudo apt-get update -y && sudo apt-get upgrade -y
sudo apt-get install --no-install-recommends software-properties-common -y
sudo add-apt-repository ppa:vbernat/haproxy-2.6
sudo apt-get install haproxy=2.6.\* -y

sudo tee -a /etc/default/haproxy > /dev/null << EOT
ENABLED=1
EOT

sudo bash -c 'cat << EOT >> /etc/haproxy/haproxy.cfg

frontend http-in
        bind *:80
        default_backend servers
backend servers
        balance roundrobin
        server web-01 54.165.180.170:80 check
        server web-02 54.210.123.212:80 check
EOT'

sudo service haproxy restart
